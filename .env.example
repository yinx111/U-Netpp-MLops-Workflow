# Optional local environment variables.
# You can leave this file absent; scripts will use defaults.
# To customize, copy to `.env` and edit values.

# ===== Training / DVC pipeline =====
DATA_ROOT=./dataset_split
BATCH_SIZE=16
EPOCHS=100
NUM_WORKERS=4
AMP=true
FORCE_CPU=1
MLFLOW_ENABLED=false
MLFLOW_TRACKING_URI=
MLFLOW_EXPERIMENT_NAME=u-net-workflow
MLFLOW_TRACKING_USERNAME=
MLFLOW_TRACKING_PASSWORD=
MIN_MIOU=0.3
MIN_OA=0.5

# ===== Inference script =====
IMG_PATH=./test_img/area_test1.tif
CHECKPOINT_PATH=./outputs/model.pth
OUT_DIR=./outputs/infer
CUDA_VISIBLE_DEVICES=

# ===== Web app / Docker compose =====
APP_HOST=0.0.0.0
APP_PORT=8000
MAX_UPLOAD_MB=200
INFERENCE_TIMEOUT_SECONDS=3600
CLOUDFLARED_TOKEN=
