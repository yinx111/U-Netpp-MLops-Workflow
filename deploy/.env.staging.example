# Remote staging deployment variables (optional if set via GitHub Environment vars).
APP_HOST=0.0.0.0
APP_PORT=8000
MAX_UPLOAD_MB=200
INFERENCE_TIMEOUT_SECONDS=3600
CHECKPOINT_PATH=/workspace/outputs/model.pth
FORCE_CPU=1
CUDA_VISIBLE_DEVICES=
