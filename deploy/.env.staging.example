# Remote staging deployment variables (optional if set via GitHub Environment vars).
APP_HOST=0.0.0.0
APP_PORT=8000
MAX_UPLOAD_MB=200
INFERENCE_TIMEOUT_SECONDS=3600
FORCE_CPU=1
CUDA_VISIBLE_DEVICES=

# CD model artifact settings (GitHub Environment vars).
MODEL_DVC_PATH=outputs/model.pth
MODEL_SHA256=
DVC_AWS_REGION=us-east-1
