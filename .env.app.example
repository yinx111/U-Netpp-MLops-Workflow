# Add these vars into your existing .env before `docker compose up -d`
CLOUDFLARED_TOKEN=your_cloudflare_tunnel_token

# App container (FastAPI + local inference subprocess)
APP_HOST=0.0.0.0
APP_PORT=8000
MAX_UPLOAD_MB=200
INFERENCE_TIMEOUT_SECONDS=3600
FORCE_CPU=1
CUDA_VISIBLE_DEVICES=
CHECKPOINT_PATH=/workspace/outputs/model.pth
